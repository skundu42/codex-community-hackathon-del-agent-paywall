"use client";

import { Form as AntForm, Input as AntInput } from "antd";

export const Form = AntForm;
export const FormItem = AntForm.Item;
export const PasswordInput = AntInput.Password;
export const TextArea = AntInput.TextArea;
